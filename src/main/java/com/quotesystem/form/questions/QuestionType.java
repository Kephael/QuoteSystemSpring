package com.quotesystem.form.questions;

/*
 * Helps REST client identify the question type.
 */
public enum QuestionType {
	BOOLEAN, LONG_RESPONSE, SHORT_RESPONSE, IMAGE, VALUE, RADIO, CHECK, NUMERIC
}
